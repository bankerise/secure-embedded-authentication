pluginManagement { includeBuild("../../../node_modules/@react-native/gradle-plugin") }
plugins { id("com.facebook.react.settings") }
extensions.configure(com.facebook.react.ReactSettingsExtension){ ex -> ex.autolinkLibrariesFromCommand() }
rootProject.name = 'demo_rn'
include ':app'

// sea-core-android is resolved from JitPack via sea-react-native's own
// build.gradle.kts (see packages/sea-react-native/android/build.gradle.kts),
// the same way demo-rn's Podfile already resolves SEACore as a published pod
// rather than a local path — this app validates the bridge the way an
// external consumer actually sees it.

includeBuild('../../../node_modules/@react-native/gradle-plugin')
